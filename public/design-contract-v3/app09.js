function scanner(){return `${topbar('entry')}<section class="scanner"><div class="scan-view"><div><div id="scanBox" class="scan-box"></div><div class="scan-hint"><b>Scanner do estado canônico</b><br><span style="font-size:13px">Aponte para um QR ou simule abaixo</span></div><div id="scanResult" class="scan-result"><span class="pill brand">ACT-001</span><h2>Validar a tela AGORA em mobile</h2><p>Objeto encontrado no mesmo projeto do Workspace.</p><div class="row" style="margin-top:14px"><button class="btn primary small" onclick="scannerAction('open')">Abrir</button><button class="btn small" onclick="scannerAction('done')">Concluir</button></div></div></div></div><div class="scan-actions"><button onclick="simulateScan()">⌗<br><small>Simular leitura</small></button><button onclick="scannerAction('register')">＋<br><small>Registrar</small></button><button onclick="setScreen('workspace')">▦<br><small>Workspace</small></button></div></section>`}
function simulateScan(){document.getElementById('scanResult').classList.add('show');toast('QR reconhecido: ACT-001')}
function scannerAction(type){if(type==='open'){state.activeActionId='ACT-001';state.onboarding=true;state.screen='workspace';save();render();setTimeout(showFocus,100)}else if(type==='done'){state.activeActionId='ACT-001';state.onboarding=true;save();setScreen('workspace');setTimeout(()=>openEvidence(true),100)}else{state.evidence.push({id:'EVD-'+String(state.evidence.length+1).padStart(3,'0'),actionId:'ACT-001',text:'Registro criado via Scanner simulado.',at:new Date().toISOString()});save();toast('Registro sincronizado com Docs')}}
function openModal(html,wide=false){const m=document.getElementById('modal');m.classList.remove('drawer-modal');document.getElementById('modalContent').innerHTML=html;m.style.maxWidth=wide?'780px':'';m.showModal()}
function closeModal(){const m=document.getElementById('modal');m.close();m.classList.remove('drawer-modal')}
function resetPrototype(){localStorage.removeItem('executarPrototype');state=structuredClone(defaults);closeModalSafe();render();toast('Protótipo reiniciado')}
function closeModalSafe(){const m=document.getElementById('modal');if(m.open)m.close()}
function esc(s=''){return String(s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}
function render(){document.body.classList.toggle('motion-none',state.motion==='none');document.body.classList.toggle('semaforo',state.color==='semaforo');const a=document.getElementById('app');const screens={entry,discovery,route,capacity,preapproval,personalization,executionModel,emissionModel,emissionCustomize,copilotSetup,workspace,scanner};a.innerHTML=(screens[state.screen]||entry)()}
render();
if('serviceWorker' in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('/sw.js').catch(()=>{}))}
let deferredPrompt=null;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;document.getElementById('installBanner').classList.add('show')});document.getElementById('installBtn').addEventListener('click',async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;document.getElementById('installBanner').classList.remove('show')});
