# @sas-executar/ui

shadcn/ui + Tailwind, consumido só por `apps/web`. Não compartilhado com `apps/mobile` — React
Native não renderiza componentes DOM/CSS diretamente; a UI do app mobile vive em
`apps/mobile` usando primitives nativos (ver `docs/PLAN.md` §1).

Sem conteúdo ainda — nasce junto com `apps/web` na Fase 0/1.
