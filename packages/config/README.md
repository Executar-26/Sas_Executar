# @sas-executar/config

Configuração compartilhada entre os apps/pacotes: `tsconfig.base.json`, config base de ESLint,
config base de Tailwind. Nasce junto com o primeiro app real (Fase 0/1) — sem conteúdo ainda,
propositalmente: não vale a pena adivinhar a config antes de haver um `apps/web` real para
validar contra.
